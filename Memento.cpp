#include "Memento.h"


    Memento::Memento(vector<Shape*> elements)
    {
        shapes = elements;
    };

    /* void deleteMemento() 
    {
        delete shapes;
        shapes = nullptr;
    };*/
